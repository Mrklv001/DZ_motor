# Служебная папка: генераторы каталога и исходники стилей.
# Наружу отдавать нечего — закрываем доступ по HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
