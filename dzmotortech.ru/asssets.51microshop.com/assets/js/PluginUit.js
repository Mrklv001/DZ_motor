// Legacy 51microshop SaaS snippet loader (fetched /snippets/js.aspx, a platform endpoint
// that no longer exists on this self-hosted static export). Disabled: depending on server
// fallback behavior, the AJAX call could receive a full HTML page back instead of a 404
// and append a duplicate copy of the page into <body>.
